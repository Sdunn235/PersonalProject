# Heartbeat-6 run-logs (per-run CSV + summary)
logs/

# Phase 1 SQLite store — runtime artifact, seeded from the canonical JSON
*.db

# Python
__pycache__/
*.py[cod]
*$py.class
.venv/
venv/

# TheForge vendored C# blueprint — source only, no build artifacts
TheForge/bin/
TheForge/obj/
TheForge/.vs/
TheForge/**/bin/
TheForge/**/obj/
